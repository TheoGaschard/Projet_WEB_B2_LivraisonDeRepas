 {% form_theme form 'bootstrap_4_layout.html.twig' %}
{{ form_start(form, { 'attr': {'style': 'padding:2%'} }) }}
        {% for item in items %}
    {{ form_row(form.quantite, {'value' : item.quantity }) }}
        {% endfor %}


{{ form_end(form) }}
